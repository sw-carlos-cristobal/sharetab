name: Auto-assign Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    # Issues can only be assigned to users; an org-owned fork or transfer skips this job
    if: github.event.repository.owner.type == 'User'
    runs-on: ubuntu-latest
    steps:
      - name: Assign new issue to repo owner
        run: gh issue edit "$NUMBER" --add-assignee "$OWNER" -R "$REPO"
        env:
          GH_TOKEN: ${{ github.token }}
          NUMBER: ${{ github.event.issue.number }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ github.repository }}
