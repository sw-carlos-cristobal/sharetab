# next-auth@5 beta (and @auth/core) declare a peerOptional on nodemailer
# "^7.0.7 || ^8.0.5" while this project uses nodemailer@9; Docker and CI
# already install with --legacy-peer-deps.
# This makes a plain `npm install` work for contributors too.
legacy-peer-deps=true
