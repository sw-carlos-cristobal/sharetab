# next-auth@5 beta declares a peerOptional on nodemailer@^7 while this project
# uses nodemailer@8; Docker and CI already install with --legacy-peer-deps.
# This makes a plain `npm install` work for contributors too.
legacy-peer-deps=true
